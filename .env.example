# ポート番号（デフォルト: 8888）
# ANY_CONSOLE_PORT=8888

# バインドホスト（デフォルト: 0.0.0.0 = 全インターフェース）
# Tailscale 等で網が閉じている場合は 0.0.0.0 のままで良い
# 認証トークン未設定で外部ネットワークに晒すと危険なため、その場合は 127.0.0.1 を推奨
# ANY_CONSOLE_HOST=127.0.0.1

# 認証トークンは UI > Security から発行する（data/auth.json に保存される）
