fastapi>=0.100,<1
uvicorn>=0.20,<1
websockets>=12,<17
pydantic>=2,<3
python-multipart>=0.0.5
cryptography>=41
# Filesystem watching for realtime git status push (api/git_watch.py).
# The app still starts without it — status push degrades to fetch/API triggers.
watchfiles>=0.21,<2

# Push notifications need pywebpush too — see requirements-optional.txt.
# It pulls a native dependency (http-ece) that can fail to build on minimal
# hosts; it is installed best-effort so a build failure never blocks setup,
# and push is lazily imported so a missing dep never blocks app startup.

# dev
pytest>=7,<9
pytest-cov>=5,<7
ruff>=0.4,<1
mypy>=1.10
httpx>=0.24,<1
